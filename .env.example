# Options: 'local' (for development) or 'cloud' (for production)
ENVIRONMENT=local

# Google Cloud Project ID
GCP_PROJECT_ID=your-project-id-here

# Server Port
# Local: 3000, Cloud Run: 8080 (auto-set by Cloud Run)
PORT=3000

# Firestore Emulator (Local Development Only)
# Leave this as-is for local development
FIRESTORE_EMULATOR_HOST=localhost:8080

# Dialogflow Integration
# Set to 'true' to enable Dialogflow NLP, 'false' for keyword matching
USE_DIALOGFLOW=true